source.dir=${buildproject}/src
gen.dir=${buildproject}/gen
resource.dir=res
out.dir=${buildproject}/bin

external.libs.dir=${buildproject}/libs
native.libs.dir=${buildproject}/libs
tested.project.absolute.dir=${buildproject}
